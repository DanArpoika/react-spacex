import styled from 'styled-components'

const PageTitle = styled.h1 `
  font-size: 4.5rem;
  width: ${7 / 12 * 100}%;
  line-height: 1.333;
  margin-bottom: 8rem;
  margin-top: 4rem;
`

export default PageTitle
